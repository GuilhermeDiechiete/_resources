

// criamos uma tarefa, e um callback para manipular e avisar o usuario que a tarefa foi concluida

app.get('/login', (req, res) => {

    // resposta da callback para o usuario
})

app.listen(3000, () => {
    // resposta em callback para avisar que o server está rodando
})